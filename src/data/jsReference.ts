import type { ReferenceCategory } from '@/types'

export const JS_REFERENCE: ReferenceCategory[] = [
  {
    id: 'variables',
    title: 'Variables & Types',
    color: '#4ade80',
    entries: [
      {
        name: 'const',
        description: 'Declares a block-scoped, read-only reference. The binding cannot be reassigned, but objects/arrays it points to can still be mutated.',
        example: 'const PI = 3.14159;\nconst user = { name: "Alice", age: 30 };\n\n// Can mutate the object:\nuser.age = 31;         // OK\nuser.role = "admin";   // OK\n\n// Cannot reassign the binding:\n// user = {};          // TypeError',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/const',
      },
      {
        name: 'let',
        description: 'Declares a block-scoped variable that can be reassigned. Prefer const; use let only when you need to reassign.',
        example: 'let count = 0;\ncount++;           // reassign is fine\n\n// Block scoped:\nif (true) {\n  let x = 10;\n  console.log(x);  // 10\n}\n// console.log(x); // ReferenceError — out of scope',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let',
      },
      {
        name: 'var (avoid)',
        description: 'Function-scoped (not block-scoped) and hoisted to the top of its function. Avoid var — use const or let to prevent scope-related bugs.',
        example: '// var leaks out of blocks:\nif (true) {\n  var x = 10;   // hoisted to function scope\n}\nconsole.log(x); // 10 (unexpected!)\n\n// let stays in block:\nif (true) {\n  let y = 10;\n}\n// console.log(y); // ReferenceError (expected)',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/var',
      },
      {
        name: 'typeof',
        description: 'Returns a string indicating the type of the operand. Note: typeof null === "object" is a historical bug in JavaScript.',
        example: 'typeof "hello"     // "string"\ntypeof 42          // "number"\ntypeof true        // "boolean"\ntypeof undefined   // "undefined"\ntypeof null        // "object" (historical bug!)\ntypeof {}          // "object"\ntypeof []          // "object"\ntypeof function(){} // "function"\ntypeof Symbol()    // "symbol"',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof',
      },
      {
        name: 'Nullish coalescing (??)',
        description: 'Returns the right-hand value when the left-hand value is null or undefined. Unlike ||, it does not treat 0, "", or false as falsy.',
        example: 'const name = null ?? "Anonymous";    // "Anonymous"\nconst count = undefined ?? 0;        // 0\nconst price = 0 ?? 99;               // 0  (0 is not nullish!)\nconst text = "" ?? "default";        // ""  (empty string is not nullish)\n\n// Contrast with || (treats 0 and "" as falsy):\nconst a = 0 || 99;    // 99 (unexpected)\nconst b = 0 ?? 99;    // 0  (correct)',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing',
      },
      {
        name: 'Optional chaining (?.)',
        description: 'Safely accesses nested properties — returns undefined instead of throwing if an intermediate value is null or undefined.',
        example: 'const user = { profile: { name: "Alice" } };\n\n// Without optional chaining — throws if profile is null:\n// const name = user.profile.name;\n\n// With optional chaining:\nconst name = user?.profile?.name;   // "Alice"\nconst city = user?.address?.city;   // undefined (no throw)\n\n// With method calls:\nconst len = user?.name?.length;     // undefined\n\n// With arrays:\nconst first = arr?.[0];',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining',
      },
      {
        name: 'Template literals',
        description: 'String literals enclosed in backticks that support embedded expressions (${}) and multi-line strings without escape sequences.',
        example: 'const name = "Alice";\nconst age = 30;\n\n// String interpolation:\nconst greeting = `Hello, ${name}! You are ${age} years old.`;\n\n// Multi-line:\nconst html = `\n  <div class="card">\n    <h2>${name}</h2>\n  </div>\n`;\n\n// Expression:\nconst result = `${2 + 2 === 4 ? "Math works" : "Uh oh"}`;',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals',
      },
      {
        name: 'Logical assignment (&&=, ||=, ??=)',
        description: 'Combines a logical operator with assignment. Only assigns when the condition is met — short-circuits like the logical operators.',
        example: '// &&= — assign if left side is truthy:\nlet user = { name: "Alice" };\nuser.name &&= user.name.trim();\n\n// ||= — assign if left side is falsy:\nlet title = "";\ntitle ||= "Untitled";\nconsole.log(title);   // "Untitled"\n\n// ??= — assign if left side is null/undefined:\nlet config = null;\nconfig ??= { theme: "dark" };\nconsole.log(config);  // { theme: "dark" }',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Logical_AND_assignment',
      },
    ],
  },
  {
    id: 'functions',
    title: 'Functions',
    color: '#5b9cf5',
    entries: [
      {
        name: 'Arrow functions',
        description: 'A concise function syntax using =>. Arrow functions do not have their own this, arguments, or super bindings — they inherit from the surrounding scope.',
        example: '// Single expression — implicit return:\nconst double = n => n * 2;\nconst add = (a, b) => a + b;\n\n// Block body — explicit return needed:\nconst greet = name => {\n  const msg = `Hello, ${name}!`;\n  return msg;\n};\n\n// Returning an object literal (wrap in parens):\nconst makeUser = name => ({ name, active: true });',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions',
      },
      {
        name: 'Default parameters',
        description: 'Function parameters can have default values that are used when no argument is provided or when undefined is passed.',
        example: 'function greet(name = "stranger", greeting = "Hello") {\n  return `${greeting}, ${name}!`;\n}\n\ngreet();                     // "Hello, stranger!"\ngreet("Alice");              // "Hello, Alice!"\ngreet("Bob", "Hi");          // "Hi, Bob!"\ngreet(undefined, "Hey");     // "Hey, stranger!"',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters',
      },
      {
        name: 'Rest parameters (...args)',
        description: 'Collects all remaining arguments into a real Array. Always the last parameter. Prefer over the legacy arguments object.',
        example: 'function sum(...numbers) {\n  return numbers.reduce((total, n) => total + n, 0);\n}\n\nsum(1, 2, 3);       // 6\nsum(1, 2, 3, 4, 5); // 15\n\n// First arg fixed, rest collected:\nfunction log(level, ...messages) {\n  messages.forEach(msg => console.log(`[${level}] ${msg}`));\n}\nlog("INFO", "Server started", "Port 3000");',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/rest_parameters',
      },
      {
        name: 'Closures',
        description: 'A function that retains access to variables from its outer (enclosing) scope even after the outer function has returned.',
        example: 'function makeCounter(start = 0) {\n  let count = start;   // captured by closure\n  return {\n    increment: () => ++count,\n    decrement: () => --count,\n    value: () => count,\n  };\n}\n\nconst counter = makeCounter(10);\ncounter.increment(); // 11\ncounter.increment(); // 12\ncounter.value();     // 12',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures',
      },
      {
        name: 'Higher-order functions',
        description: 'Functions that take other functions as arguments or return a function. The basis of callbacks, array methods, and functional patterns.',
        example: '// Takes a function:\nfunction repeat(n, fn) {\n  for (let i = 0; i < n; i++) fn(i);\n}\nrepeat(3, i => console.log(`Step ${i}`));\n\n// Returns a function (factory):\nfunction multiplier(factor) {\n  return n => n * factor;\n}\nconst double = multiplier(2);\nconst triple = multiplier(3);\ndouble(5); // 10\ntriple(5); // 15',
        link: 'https://developer.mozilla.org/en-US/docs/Glossary/First-class_Function',
      },
      {
        name: 'Function.prototype.bind',
        description: 'Creates a new function with a fixed this value and optionally pre-filled arguments (partial application).',
        example: 'const user = { name: "Alice" };\n\nfunction greet(greeting, punctuation) {\n  return `${greeting}, ${this.name}${punctuation}`;\n}\n\n// Bind this + partial application:\nconst greetAlice = greet.bind(user, "Hello");\ngreetAlice("!");    // "Hello, Alice!"\ngreetAlice("?");    // "Hello, Alice?"\n\n// .call — invoke immediately with this:\ngreet.call(user, "Hi", ".");\n\n// .apply — invoke with this + args as array:\ngreet.apply(user, ["Hey", "!"]);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/bind',
      },
    ],
  },
  {
    id: 'arrays',
    title: 'Arrays',
    color: '#f59e0b',
    entries: [
      {
        name: 'Array.map()',
        description: 'Creates a new array by applying a function to every element of the original. Does not mutate the source array.',
        example: 'const numbers = [1, 2, 3, 4, 5];\n\nconst doubled = numbers.map(n => n * 2);\n// [2, 4, 6, 8, 10]\n\nconst users = [{name:"Alice"},{name:"Bob"}];\nconst names = users.map(u => u.name);\n// ["Alice", "Bob"]',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/map',
      },
      {
        name: 'Array.filter()',
        description: 'Creates a new array containing only elements that pass the test function. Does not mutate the original.',
        example: 'const numbers = [1, 2, 3, 4, 5, 6];\n\nconst evens = numbers.filter(n => n % 2 === 0);\n// [2, 4, 6]\n\nconst users = [\n  { name: "Alice", active: true },\n  { name: "Bob", active: false },\n  { name: "Carol", active: true },\n];\nconst active = users.filter(u => u.active);\n// [{ name: "Alice"... }, { name: "Carol"... }]',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/filter',
      },
      {
        name: 'Array.reduce()',
        description: 'Executes a reducer function on each element to produce a single accumulated value. Provide an initial value to avoid bugs on empty arrays.',
        example: 'const numbers = [1, 2, 3, 4, 5];\n\n// Sum:\nconst sum = numbers.reduce((acc, n) => acc + n, 0); // 15\n\n// Group by:\nconst words = ["cat", "car", "bar", "bat"];\nconst byLetter = words.reduce((acc, word) => {\n  const key = word[0];\n  (acc[key] ??= []).push(word);\n  return acc;\n}, {});\n// { c: ["cat","car"], b: ["bar","bat"] }',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/reduce',
      },
      {
        name: 'Array.find() / findIndex()',
        description: 'find() returns the first element that passes the test; findIndex() returns its index. Both return undefined/-1 if nothing matches.',
        example: 'const users = [\n  { id: 1, name: "Alice" },\n  { id: 2, name: "Bob" },\n  { id: 3, name: "Carol" },\n];\n\nconst bob = users.find(u => u.id === 2);\n// { id: 2, name: "Bob" }\n\nconst idx = users.findIndex(u => u.name === "Carol");\n// 2',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/find',
      },
      {
        name: 'Array.flat() / flatMap()',
        description: 'flat() flattens nested arrays by depth (default: 1). flatMap() maps then flattens one level — more efficient than chaining map + flat.',
        example: 'const nested = [1, [2, 3], [4, [5, 6]]];\nnested.flat();    // [1, 2, 3, 4, [5, 6]]\nnested.flat(2);   // [1, 2, 3, 4, 5, 6]\nnested.flat(Infinity); // fully flatten\n\nconst sentences = ["hello world", "foo bar"];\nconst words = sentences.flatMap(s => s.split(" "));\n// ["hello", "world", "foo", "bar"]',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/flat',
      },
      {
        name: 'Array spread / destructuring',
        description: 'Spread (...) expands an array into individual elements. Destructuring extracts values into variables.',
        example: '// Spread — copy, merge, pass as args:\nconst a = [1, 2, 3];\nconst b = [...a, 4, 5];    // [1, 2, 3, 4, 5]\nconst copy = [...a];        // shallow copy\n\n// Destructuring:\nconst [first, second, ...rest] = [10, 20, 30, 40];\nconsole.log(first);  // 10\nconsole.log(rest);   // [30, 40]\n\n// Swap variables:\nlet x = 1, y = 2;\n[x, y] = [y, x];  // x=2, y=1',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment',
      },
      {
        name: 'Array.includes() / some() / every()',
        description: 'includes() checks for a value; some() checks if any element passes a test; every() checks if all elements pass.',
        example: 'const arr = [1, 2, 3, 4, 5];\n\narr.includes(3);             // true\narr.includes(6);             // false\n\narr.some(n => n > 4);        // true  (5 > 4)\narr.every(n => n > 0);       // true  (all positive)\narr.every(n => n > 3);       // false (1,2,3 fail)\n\n// Check if any role is admin:\nconst roles = ["user", "editor", "admin"];\nroles.includes("admin");     // true',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/includes',
      },
      {
        name: 'Array.sort()',
        description: 'Sorts elements in place and returns the array. Without a comparator, elements are sorted as strings. Always provide a comparator for numbers.',
        example: '// String sort (default):\n["banana", "apple", "cherry"].sort();\n// ["apple", "banana", "cherry"]\n\n// Numeric sort — comparator required:\n[10, 2, 1, 20].sort((a, b) => a - b);\n// [1, 2, 10, 20]  ascending\n\n[10, 2, 1, 20].sort((a, b) => b - a);\n// [20, 10, 2, 1]  descending\n\n// Non-mutating (ES2023):\nconst sorted = [3,1,2].toSorted((a,b)=>a-b);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort',
      },
    ],
  },
  {
    id: 'objects',
    title: 'Objects',
    color: '#a78bfa',
    entries: [
      {
        name: 'Object destructuring',
        description: 'Extracts properties from objects into distinct variables. Supports renaming, default values, and nested destructuring.',
        example: 'const user = { name: "Alice", age: 30, role: "admin" };\n\n// Basic:\nconst { name, age } = user;\n\n// Rename:\nconst { name: userName } = user;\n\n// Default value:\nconst { role = "user" } = user;\n\n// Nested:\nconst { address: { city } = {} } = user;\n\n// In function params:\nfunction greet({ name, role = "guest" }) {\n  return `Hello, ${name} (${role})`;\n}',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment',
      },
      {
        name: 'Object spread',
        description: 'Copies own enumerable properties from one object into another. Last duplicate key wins. Creates a shallow copy.',
        example: 'const defaults = { theme: "dark", lang: "en" };\nconst override = { theme: "light" };\n\n// Merge (later keys win):\nconst config = { ...defaults, ...override };\n// { theme: "light", lang: "en" }\n\n// Shallow copy:\nconst copy = { ...defaults };\n\n// Add properties:\nconst withLocale = { ...defaults, locale: "de" };',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Spread_syntax',
      },
      {
        name: 'Object.keys / values / entries',
        description: 'Object.keys() returns an array of own property names; values() returns values; entries() returns [key, value] pairs.',
        example: 'const obj = { a: 1, b: 2, c: 3 };\n\nObject.keys(obj);    // ["a", "b", "c"]\nObject.values(obj);  // [1, 2, 3]\nObject.entries(obj); // [["a",1],["b",2],["c",3]]\n\n// Iterate with for...of:\nfor (const [key, value] of Object.entries(obj)) {\n  console.log(`${key}: ${value}`);\n}\n\n// Convert back from entries:\nObject.fromEntries([["x", 1], ["y", 2]]); // { x:1, y:2 }',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/keys',
      },
      {
        name: 'Object.assign()',
        description: 'Copies enumerable own properties from one or more source objects into a target. Returns the target. Mutates the target (use spread for immutable copy).',
        example: '// Merge into existing object (mutates target):\nconst target = { a: 1 };\nObject.assign(target, { b: 2 }, { c: 3 });\n// target is now { a: 1, b: 2, c: 3 }\n\n// Shallow clone (prefer spread):\nconst clone = Object.assign({}, target);\n\n// Set defaults without mutation:\nconst opts = Object.assign({}, defaults, userOpts);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/assign',
      },
      {
        name: 'Optional chaining on objects',
        description: 'Safely access deeply nested object properties without throwing if any intermediate value is null or undefined.',
        example: 'const response = {\n  data: {\n    user: { name: "Alice", address: null }\n  }\n};\n\nconst name = response?.data?.user?.name;    // "Alice"\nconst city = response?.data?.user?.address?.city;  // undefined\n\n// Method calls:\nconst len = response?.data?.items?.length;  // undefined\n\n// Array index:\nconst first = response?.data?.items?.[0];   // undefined',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining',
      },
      {
        name: 'JSON.stringify / parse',
        description: 'JSON.stringify() converts a value to a JSON string; JSON.parse() parses a JSON string back into a value.',
        example: 'const user = { name: "Alice", age: 30 };\n\n// To JSON string:\nconst json = JSON.stringify(user);\n// \'{"name":"Alice","age":30}\'\n\n// Pretty print:\nJSON.stringify(user, null, 2);\n\n// Parse back:\nconst parsed = JSON.parse(json);\n// { name: "Alice", age: 30 }\n\n// Quick shallow clone (loses methods/undefined):\nconst clone = JSON.parse(JSON.stringify(user));\n\n// Better: structuredClone():\nconst deep = structuredClone(user);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify',
      },
    ],
  },
  {
    id: 'strings',
    title: 'Strings',
    color: '#fb923c',
    entries: [
      {
        name: 'String methods: search',
        description: 'Methods for finding and testing substrings: includes(), startsWith(), endsWith(), indexOf(), search().',
        example: 'const str = "Hello, JavaScript!";\n\nstr.includes("Java");       // true\nstr.startsWith("Hello");    // true\nstr.endsWith("!");          // true\nstr.indexOf("a");           // 7  (first occurrence)\nstr.lastIndexOf("a");       // 13 (last occurrence)\n\n// Regex search:\nstr.search(/java/i);        // 7 (case-insensitive)',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/includes',
      },
      {
        name: 'String methods: extract',
        description: 'Methods for extracting substrings: slice(), substring(), at(), split().',
        example: 'const str = "Hello, World!";\n\nstr.slice(7, 12);      // "World"\nstr.slice(-6);         // "orld!"\nstr.substring(7, 12);  // "World" (no negative index)\nstr.at(0);             // "H"\nstr.at(-1);            // "!" (last char)\n\nstr.split(", ");       // ["Hello", "World!"]\n"a-b-c".split("-");    // ["a", "b", "c"]',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/slice',
      },
      {
        name: 'String methods: transform',
        description: 'Methods for transforming strings: replace(), replaceAll(), trim(), padStart(), padEnd(), repeat(), toUpperCase(), toLowerCase().',
        example: '"hello world".toUpperCase()       // "HELLO WORLD"\n"HELLO WORLD".toLowerCase()       // "hello world"\n"  hello  ".trim()                // "hello"\n"  hello  ".trimStart()          // "hello  "\n"hello world".replace("world","JS") // "hello JS"\n"aabbcc".replaceAll("b","X")      // "aaXXcc"\n"5".padStart(3, "0")              // "005"\n"hi".padEnd(5, "!")               // "hi!!!"\n"abc".repeat(3)                   // "abcabcabc"',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/replace',
      },
      {
        name: 'String.match / matchAll',
        description: 'match() returns the first match (or all with /g flag); matchAll() returns an iterator of all matches with capture groups.',
        example: 'const str = "cat, bat, sat";\n\n// First match:\nstr.match(/[a-z]at/);    // ["cat", index: 0, ...]\n\n// All matches with /g:\nstr.match(/[a-z]at/g);   // ["cat", "bat", "sat"]\n\n// matchAll with capture groups:\nconst html = "<h1>Title</h1><h2>Sub</h2>";\nconst matches = [...html.matchAll(/<(h[1-6])>(.*?)<\\/\\1>/g)];\nmatches[0][1]; // "h1"\nmatches[0][2]; // "Title"',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/match',
      },
    ],
  },
  {
    id: 'async',
    title: 'Async & Promises',
    color: '#f472b6',
    entries: [
      {
        name: 'Promises',
        description: 'An object representing the eventual completion or failure of an asynchronous operation. A Promise is in one of three states: pending, fulfilled, or rejected.',
        example: '// Creating a promise:\nconst fetchData = new Promise((resolve, reject) => {\n  setTimeout(() => resolve("data"), 1000);\n});\n\n// Consuming:\nfetchData\n  .then(data => console.log(data))   // "data"\n  .catch(err => console.error(err))\n  .finally(() => console.log("done"));\n\n// Shorthand:\nPromise.resolve(42).then(v => console.log(v));\nPromise.reject(new Error("oops")).catch(console.error);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise',
      },
      {
        name: 'async / await',
        description: 'async functions always return a Promise. await pauses execution until the Promise settles. Makes async code look and behave like synchronous code.',
        example: 'async function fetchUser(id) {\n  try {\n    const response = await fetch(`/api/users/${id}`);\n    if (!response.ok) throw new Error(`HTTP ${response.status}`);\n    const user = await response.json();\n    return user;\n  } catch (err) {\n    console.error("Failed:", err.message);\n    throw err;\n  }\n}\n\n// Call it:\nconst user = await fetchUser(1);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function',
      },
      {
        name: 'Promise.all / allSettled / race / any',
        description: 'Static methods that handle multiple Promises. all() fails fast; allSettled() always collects results; race() resolves with the first; any() resolves with the first success.',
        example: '// all: fails if any reject\nconst [users, posts] = await Promise.all([\n  fetch("/api/users").then(r => r.json()),\n  fetch("/api/posts").then(r => r.json()),\n]);\n\n// allSettled: always resolves with all results\nconst results = await Promise.allSettled([p1, p2, p3]);\nresults.forEach(r => {\n  if (r.status === "fulfilled") console.log(r.value);\n  else console.error(r.reason);\n});\n\n// any: first success wins\nconst first = await Promise.any([mirror1, mirror2, mirror3]);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/all',
      },
      {
        name: 'fetch',
        description: 'The modern API for making HTTP requests. Returns a Promise. Remember to check response.ok — fetch does not reject on 4xx/5xx status codes.',
        example: '// GET request:\nconst res = await fetch("https://api.example.com/users");\nif (!res.ok) throw new Error(`HTTP error: ${res.status}`);\nconst users = await res.json();\n\n// POST request:\nconst res2 = await fetch("/api/users", {\n  method: "POST",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify({ name: "Alice" }),\n});\nconst created = await res2.json();',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch',
      },
      {
        name: 'setTimeout / setInterval',
        description: 'setTimeout() runs a function once after a delay; setInterval() runs it repeatedly at a fixed interval. Both return an ID to cancel with clearTimeout/clearInterval.',
        example: '// One-time delay:\nconst id = setTimeout(() => {\n  console.log("Runs once after 1s");\n}, 1000);\nclearTimeout(id); // cancel if needed\n\n// Repeating:\nconst intervalId = setInterval(() => {\n  console.log("Runs every 2s");\n}, 2000);\n\n// Stop after 10 seconds:\nsetTimeout(() => clearInterval(intervalId), 10000);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/setTimeout',
      },
      {
        name: 'AbortController',
        description: 'Cancels fetch requests or any Promise-based operation. Pass the signal to fetch; call abort() to cancel.',
        example: 'const controller = new AbortController();\nconst { signal } = controller;\n\n// Pass signal to fetch:\ntry {\n  const res = await fetch("/api/data", { signal });\n  const data = await res.json();\n} catch (err) {\n  if (err.name === "AbortError") {\n    console.log("Request cancelled");\n  }\n}\n\n// Cancel after 5 seconds:\nconst timeout = setTimeout(() => controller.abort(), 5000);\n\n// Cancel on user action:\ncancelBtn.onclick = () => controller.abort();',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/AbortController',
      },
    ],
  },
  {
    id: 'dom',
    title: 'DOM & Events',
    color: '#34d399',
    entries: [
      {
        name: 'querySelector / querySelectorAll',
        description: 'querySelector() returns the first matching element; querySelectorAll() returns a NodeList of all matches. Both accept any CSS selector.',
        example: '// First match:\nconst btn = document.querySelector(".btn-primary");\nconst input = document.querySelector("input[type=email]");\nconst heading = document.querySelector("h1");\n\n// All matches:\nconst cards = document.querySelectorAll(".card");\ncards.forEach(card => card.classList.add("loaded"));\n\n// Scoped to a parent:\nconst nav = document.querySelector("nav");\nnav.querySelectorAll("a");  // only links inside nav',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/Document/querySelector',
      },
      {
        name: 'createElement / appendChild / append',
        description: 'createElement() creates a new element; appendChild() adds it as the last child; append() is more flexible — accepts strings and multiple nodes.',
        example: '// Create and add an element:\nconst li = document.createElement("li");\nli.textContent = "New item";\nli.className = "list-item";\ndocument.querySelector("ul").appendChild(li);\n\n// append() with text and elements:\nconst div = document.createElement("div");\ndiv.append("Hello ", document.createElement("strong"));\n\n// prepend() adds to the beginning:\nlist.prepend(li);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/Document/createElement',
      },
      {
        name: 'element.classList',
        description: 'Provides methods to manipulate CSS classes: add, remove, toggle, contains, replace. Preferred over manually editing className string.',
        example: 'const el = document.querySelector(".card");\n\nel.classList.add("active");           // add\nel.classList.remove("hidden");        // remove\nel.classList.toggle("expanded");      // add if absent, remove if present\nel.classList.toggle("open", true);    // force add\nel.classList.contains("active");      // true/false\nel.classList.replace("old", "new");   // replace one class\n\n// Multiple at once:\nel.classList.add("one", "two", "three");',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/Element/classList',
      },
      {
        name: 'addEventListener',
        description: 'Registers an event handler on an element. Supports multiple handlers for the same event — unlike setting onclick = fn which can only have one.',
        example: 'const btn = document.querySelector("#submit");\n\n// Basic:\nbtn.addEventListener("click", (e) => {\n  console.log("Clicked!", e.target);\n});\n\n// Remove later:\nfunction handleClick(e) { ... }\nbtn.addEventListener("click", handleClick);\nbtn.removeEventListener("click", handleClick);\n\n// Options:\nbtn.addEventListener("click", handler, { once: true });\nbtn.addEventListener("click", handler, { passive: true });',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener',
      },
      {
        name: 'event.preventDefault / stopPropagation',
        description: 'preventDefault() cancels the browser\'s default action; stopPropagation() prevents the event from bubbling up to parent elements.',
        example: '// Prevent form submission / link navigation:\nform.addEventListener("submit", (e) => {\n  e.preventDefault();\n  // handle with JS\n});\n\n// Prevent link navigation:\nlink.addEventListener("click", (e) => {\n  e.preventDefault();\n  navigateSPA(e.target.href);\n});\n\n// Stop bubbling (child click doesn\'t trigger parent):\nchild.addEventListener("click", (e) => {\n  e.stopPropagation();\n});',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/Event/preventDefault',
      },
      {
        name: 'IntersectionObserver',
        description: 'Observes when elements enter or leave the viewport. Efficient alternative to scroll event listeners for lazy loading, animations, and infinite scroll.',
        example: 'const observer = new IntersectionObserver(\n  (entries) => {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        entry.target.classList.add("visible");\n        observer.unobserve(entry.target); // stop after first\n      }\n    });\n  },\n  {\n    threshold: 0.1,     // 10% visible triggers\n    rootMargin: "0px 0px -50px 0px",\n  }\n);\n\ndocument.querySelectorAll(".fade-in")\n  .forEach(el => observer.observe(el));',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/IntersectionObserver',
      },
    ],
  },
  {
    id: 'modules',
    title: 'Modules & Classes',
    color: '#e879f9',
    entries: [
      {
        name: 'import / export',
        description: 'ES modules — named exports are explicitly listed; default export is the main export of a module. Both can be used in the same file.',
        example: '// Named exports:\nexport const PI = 3.14;\nexport function add(a, b) { return a + b; }\n\n// Default export:\nexport default class Calculator { ... }\n\n// Importing:\nimport Calculator from "./calculator.js";       // default\nimport { PI, add } from "./math.js";            // named\nimport { add as sum } from "./math.js";         // alias\nimport * as math from "./math.js";              // namespace\nimport Calculator, { PI } from "./module.js";  // both',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/import',
      },
      {
        name: 'Dynamic import()',
        description: 'Lazily loads a module at runtime, returning a Promise. Use for code splitting and conditional loading.',
        example: '// Load only when needed:\nbtn.addEventListener("click", async () => {\n  const { openModal } = await import("./modal.js");\n  openModal();\n});\n\n// Conditional loading:\nif (userPreferences.charts) {\n  const Chart = (await import("./chart.js")).default;\n  new Chart("#canvas");\n}\n\n// Always returns a module object:\nconst module = await import("./utils.js");\nmodule.helper();',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/import',
      },
      {
        name: 'Class syntax',
        description: 'Syntactic sugar over prototype-based inheritance. Classes support constructor, public/private fields, static members, and extends.',
        example: 'class Animal {\n  #sound;   // private field\n  species;  // public field\n\n  constructor(name, sound) {\n    this.name = name;\n    this.#sound = sound;\n  }\n\n  speak() {\n    return `${this.name} says ${this.#sound}`;\n  }\n\n  static create(name) {\n    return new Animal(name, "...");\n  }\n}\n\nclass Dog extends Animal {\n  constructor(name) {\n    super(name, "woof");\n  }\n  fetch() { return "fetching!"; }\n}',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes',
      },
      {
        name: 'Map',
        description: 'A key-value store where keys can be any type (not just strings). Maintains insertion order and has a .size property.',
        example: 'const map = new Map();\n\n// Any type as key:\nmap.set("name", "Alice");\nmap.set(42, "the answer");\nmap.set(true, "boolean key");\n\nmap.get("name");     // "Alice"\nmap.has(42);         // true\nmap.delete(true);\nmap.size;            // 2\n\n// Iterate:\nfor (const [key, value] of map) {\n  console.log(key, "→", value);\n}\n\n// From entries:\nconst m = new Map([["a", 1], ["b", 2]]);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map',
      },
      {
        name: 'Set',
        description: 'A collection of unique values of any type. Maintains insertion order. Great for deduplication and membership checks.',
        example: 'const set = new Set([1, 2, 3, 2, 1]);\n// Set {1, 2, 3} — duplicates removed\n\nset.add(4);\nset.has(3);         // true\nset.delete(2);\nset.size;           // 3\n\n// Deduplicate an array:\nconst unique = [...new Set([1, 2, 2, 3, 3])];\n// [1, 2, 3]\n\n// Iterate:\nfor (const value of set) {\n  console.log(value);\n}',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set',
      },
    ],
  },
  {
    id: 'modern-js',
    title: 'Modern JS Features',
    color: '#94a3b8',
    entries: [
      {
        name: 'structuredClone()',
        description: 'Creates a deep clone of a value — handles nested objects, arrays, Date, Map, Set, and more. Better than JSON.parse(JSON.stringify()) for most cases.',
        example: 'const original = {\n  name: "Alice",\n  scores: [95, 87, 92],\n  date: new Date(),\n};\n\n// Deep clone:\nconst copy = structuredClone(original);\ncopy.scores.push(100);  // does not affect original\n\n// JSON approach fails for Date, Map, Set, undefined:\n// const bad = JSON.parse(JSON.stringify(obj));\n// bad.date is a string, not a Date',
        link: 'https://developer.mozilla.org/en-US/docs/Web/API/structuredClone',
      },
      {
        name: 'Array.at() / String.at()',
        description: 'Accesses an element by index, supporting negative indices from the end. Cleaner than arr[arr.length - 1].',
        example: 'const arr = [10, 20, 30, 40, 50];\n\narr.at(0);    // 10   (first)\narr.at(-1);   // 50   (last)\narr.at(-2);   // 40   (second to last)\n\nconst str = "Hello";\nstr.at(0);    // "H"\nstr.at(-1);   // "o"\n\n// Especially useful in chained expressions:\nconst lastUser = getUsers().at(-1);',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/at',
      },
      {
        name: 'Object.groupBy()',
        description: 'Groups array elements into an object based on a key returned by the callback function. Available in modern browsers (ES2024).',
        example: 'const products = [\n  { name: "Apple", category: "fruit" },\n  { name: "Banana", category: "fruit" },\n  { name: "Carrot", category: "vegetable" },\n  { name: "Broccoli", category: "vegetable" },\n];\n\nconst grouped = Object.groupBy(products, p => p.category);\n// {\n//   fruit: [{name:"Apple",...},{name:"Banana",...}],\n//   vegetable: [{name:"Carrot",...},{...}]\n// }',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/groupBy',
      },
      {
        name: 'for...of loop',
        description: 'Iterates over any iterable: arrays, strings, Maps, Sets, NodeLists. Unlike for...in, it iterates values, not property keys.',
        example: '// Array:\nconst nums = [1, 2, 3];\nfor (const n of nums) console.log(n);\n\n// String (character by character):\nfor (const char of "hello") console.log(char);\n\n// Map entries:\nconst map = new Map([["a", 1], ["b", 2]]);\nfor (const [key, val] of map) console.log(key, val);\n\n// With index using entries():\nfor (const [i, val] of nums.entries()) {\n  console.log(i, val);\n}',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for...of',
      },
      {
        name: 'Array toSorted / toReversed / toSpliced',
        description: 'Non-mutating versions of sort, reverse, and splice (ES2023). Return a new array without modifying the original.',
        example: 'const nums = [3, 1, 4, 1, 5];\n\n// Non-mutating sort:\nconst sorted = nums.toSorted((a, b) => a - b);\n// [1, 1, 3, 4, 5] — nums unchanged\n\n// Non-mutating reverse:\nconst reversed = nums.toReversed();\n// [5, 1, 4, 1, 3] — nums unchanged\n\n// Non-mutating splice (replace element):\nconst replaced = nums.toSpliced(2, 1, 99);\n// [3, 1, 99, 1, 5] — nums unchanged\n\nconsole.log(nums); // [3, 1, 4, 1, 5] (unchanged)',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/toSorted',
      },
      {
        name: 'Error handling patterns',
        description: 'Always catch specific error types; use finally for cleanup; avoid catching and rethrowing without adding context.',
        example: 'async function fetchUser(id) {\n  let response;\n  try {\n    response = await fetch(`/api/users/${id}`);\n    if (!response.ok) {\n      throw new Error(`HTTP ${response.status}: ${response.statusText}`);\n    }\n    return await response.json();\n  } catch (err) {\n    if (err.name === "AbortError") return null;  // cancelled\n    console.error("fetchUser failed:", err.message);\n    throw err;  // re-throw for caller to handle\n  } finally {\n    // runs always — good for cleanup\n    console.log("request complete");\n  }\n}',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/try...catch',
      },
    ],
  },
  {
    id: 'date',
    title: 'Date',
    color: '#f97316',
    entries: [
      {
        name: 'new Date() / Date.now()',
        description: 'Create Date objects representing a point in time. Date.now() returns the current timestamp in milliseconds — useful for performance measurement.',
        example: 'const now = new Date()           // current date & time\nconst d1 = new Date("2024-01-15")  // from ISO string\nconst d2 = new Date(2024, 0, 15)   // year, month (0-based), day\nconst d3 = new Date(1705363200000) // from timestamp\n\n// Current timestamp (ms since epoch):\nconst ts = Date.now()  // e.g. 1705363200000\n\n// Performance timing:\nconst start = Date.now()\ndoHeavyWork()\nconsole.log(`took ${Date.now() - start}ms`)',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date',
      },
      {
        name: 'Date get methods',
        description: 'Extract individual components of a date. Month is 0-based (0 = January, 11 = December). Use getFullYear(), not getYear().',
        example: 'const d = new Date("2024-03-15T10:30:00")\n\nd.getFullYear()     // 2024\nd.getMonth()        // 2  (March = index 2!)\nd.getDate()         // 15 (day of month)\nd.getDay()          // 5  (0=Sun, 1=Mon … 6=Sat)\nd.getHours()        // 10\nd.getMinutes()      // 30\nd.getSeconds()      // 0\nd.getTime()         // ms since epoch\nd.getTimezoneOffset() // difference from UTC in minutes',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/getFullYear',
      },
      {
        name: 'Date formatting',
        description: 'Convert dates to readable strings. toISOString() gives a standard format; toLocaleDateString() formats for the user\'s locale.',
        example: 'const d = new Date("2024-03-15T10:30:00Z")\n\nd.toISOString()          // "2024-03-15T10:30:00.000Z"\nd.toLocaleDateString()   // "3/15/2024" (locale-dependent)\nd.toLocaleTimeString()   // "10:30:00 AM"\nd.toLocaleString()       // "3/15/2024, 10:30:00 AM"\nd.toDateString()         // "Fri Mar 15 2024"\n\n// Custom locale + options:\nd.toLocaleDateString("de-DE", {\n  day: "2-digit",\n  month: "long",\n  year: "numeric",\n})  // "15. März 2024"',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toLocaleDateString',
      },
      {
        name: 'Date comparison & arithmetic',
        description: 'Dates can be compared using their timestamp. Subtract two dates to get the difference in milliseconds.',
        example: 'const a = new Date("2024-01-01")\nconst b = new Date("2024-03-15")\n\n// Compare:\na < b    // true\na > b    // false\na.getTime() === b.getTime()  // strict equality\n\n// Difference:\nconst diffMs = b - a   // milliseconds\nconst diffDays = Math.floor(diffMs / (1000 * 60 * 60 * 24)) // 74\n\n// Add 7 days:\nconst next = new Date(a)\nnext.setDate(next.getDate() + 7)\n\n// Days until event:\nconst daysLeft = Math.ceil((b - Date.now()) / (1000 * 60 * 60 * 24))',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date',
      },
    ],
  },
  {
    id: 'regexp',
    title: 'Regular Expressions',
    color: '#a78bfa',
    entries: [
      {
        name: 'RegExp literals & flags',
        description: 'Regular expressions are patterns for matching strings. Use /pattern/flags syntax or new RegExp(). Key flags: g (global), i (case-insensitive), m (multiline), s (dotAll).',
        example: '// Literal syntax:\nconst re = /hello/i              // case-insensitive\nconst reGlobal = /\\d+/g          // all digit sequences\n\n// Constructor (for dynamic patterns):\nconst term = "error"\nconst re2 = new RegExp(term, "i")\n\n// Test if pattern exists:\n/^\\d{4}$/.test("2024")           // true\n/^\\d{4}$/.test("abc")            // false\n\n// Flags:\n// g  — find all matches, not just first\n// i  — case-insensitive\n// m  — ^ and $ match line start/end\n// s  — . matches newline too',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_expressions',
      },
      {
        name: 'Character classes & quantifiers',
        description: 'Character classes match sets of characters. Quantifiers control how many times a pattern repeats.',
        example: '// Character classes:\n/\\d/     // any digit [0-9]\n/\\w/     // word char [a-zA-Z0-9_]\n/\\s/     // whitespace\n/\\D/     // non-digit\n/[aeiou]/ // any vowel\n/[^0-9]/  // NOT a digit\n/[a-z]/   // lowercase a-z\n\n// Quantifiers:\n/a+/     // one or more a\n/a*/     // zero or more a\n/a?/     // zero or one a\n/a{3}/   // exactly 3 a\'s\n/a{2,4}/ // 2 to 4 a\'s\n\n// Anchors:\n/^start/ // must be at line start\n/end$/   // must be at line end',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_expressions/Character_classes',
      },
      {
        name: 'Groups & match',
        description: 'Groups capture matched substrings. Named groups (?<name>) are clearer than numbered groups. String.match() and matchAll() extract results.',
        example: '// Numbered group:\nconst m = "2024-03-15".match(/(\\d{4})-(\\d{2})-(\\d{2})/)\nm[1]  // "2024"\nm[2]  // "03"\nm[3]  // "15"\n\n// Named groups (preferred):\nconst { groups } = "2024-03-15".match(\n  /(?<year>\\d{4})-(?<month>\\d{2})-(?<day>\\d{2})/\n)\ngroups.year   // "2024"\ngroups.month  // "03"\n\n// All matches (global flag):\nconst emails = "a@x.com b@y.org"\n  .matchAll(/[\\w.]+@[\\w.]+/g)\nfor (const m of emails) console.log(m[0])',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/exec',
      },
      {
        name: 'replace / replaceAll with regex',
        description: 'Use regex in replace() for powerful string transformations. Capture groups can be referenced in the replacement string with $1, $2 or named $<name>.',
        example: '// Basic replace (first match only without g):\n"hello world".replace(/o/, "0")   // "hell0 world"\n\n// Replace all with global flag:\n"hello world".replace(/o/g, "0")  // "hell0 w0rld"\n\n// Use captured group in replacement:\n"John Smith".replace(/(\\w+) (\\w+)/, "$2, $1")\n// "Smith, John"\n\n// Named group reference:\n"2024-03-15".replace(\n  /(?<y>\\d{4})-(?<m>\\d{2})-(?<d>\\d{2})/,\n  "$<d>.$<m>.$<y>"\n)  // "15.03.2024"\n\n// Function as replacer:\n"hello world".replace(/\\b\\w/g, c => c.toUpperCase())\n// "Hello World"',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/replace',
      },
    ],
  },
  {
    id: 'math-number',
    title: 'Math & Number',
    color: '#34d399',
    entries: [
      {
        name: 'Math methods',
        description: 'Built-in Math object with static methods for rounding, random numbers, and trigonometry. Math.random() returns [0, 1).',
        example: 'Math.floor(4.7)     // 4   (round down)\nMath.ceil(4.1)      // 5   (round up)\nMath.round(4.5)     // 5   (nearest integer)\nMath.abs(-10)       // 10  (absolute value)\nMath.max(1, 5, 3)   // 5\nMath.min(1, 5, 3)   // 1\nMath.pow(2, 10)     // 1024\nMath.sqrt(16)       // 4\nMath.trunc(4.9)     // 4   (drop decimal)\nMath.sign(-5)       // -1  (-1, 0, or 1)\nMath.log2(8)        // 3\nMath.PI             // 3.14159…\n\n// Random integer between min and max (inclusive):\nconst rand = (min, max) => Math.floor(Math.random() * (max - min + 1)) + min\nrand(1, 6)   // dice roll',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math',
      },
      {
        name: 'Number methods & formatting',
        description: 'Format numbers for display with toFixed, toPrecision, and toLocaleString. Check for valid numbers with Number.isNaN and Number.isFinite.',
        example: 'const n = 1234.5678\n\nn.toFixed(2)         // "1234.57" (2 decimal places)\nn.toPrecision(6)     // "1234.57" (6 significant digits)\nn.toFixed(0)         // "1235"\n\n// Locale formatting:\nn.toLocaleString("de-DE")  // "1.234,568"\n\n// Currency:\n(1299.99).toLocaleString("en-US", {\n  style: "currency",\n  currency: "USD",\n})  // "$1,299.99"\n\n// Validation:\nNumber.isNaN(NaN)         // true\nNumber.isNaN("hello")     // false (unlike global isNaN)\nNumber.isFinite(Infinity) // false\nNumber.isInteger(4.0)     // true\nNumber.isInteger(4.5)     // false',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number',
      },
      {
        name: 'parseInt / parseFloat / Number()',
        description: 'Parse strings to numbers. parseInt stops at non-numeric characters; Number() is strict — returns NaN for invalid input. Always pass the radix (base) to parseInt.',
        example: 'parseInt("42px")       // 42   (stops at "p")\nparseInt("0xFF", 16)   // 255  (hex)\nparseInt("10", 2)      // 2    (binary)\nparseInt("3.9")        // 3    (no rounding)\nparseFloat("3.14rem")  // 3.14 (stops at "r")\n\nNumber("42")           // 42\nNumber("3.14")         // 3.14\nNumber("")             // 0\nNumber("42px")         // NaN  (strict)\nNumber(true)           // 1\nNumber(false)          // 0\nNumber(null)           // 0\nNumber(undefined)      // NaN\n\n// Prefer Number() for strict conversion:\nconst input = "42"\nconsole.log(Number.isNaN(Number(input)))  // false = valid',
        link: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/parseInt',
      },
    ],
  },
]
